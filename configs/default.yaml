project:
  name: "aiops-hdfs"
  seed: 1337

paths:
  # Keep your existing dataset folder as the source of truth for now.
  # Later, if you migrate to data/raw/hdfs/, you only change this config.
  raw_data_dir: "Data/raw"
  processed_data_dir: "data/processed"
  artifacts_dir: "artifacts"
  splits_dir: "splits"
  reports_dir: "reports"
  models_dir: "models"

data_files:
  event_traces: "Event_traces.csv"
  anomaly_labels: "anomaly_label.csv"
  hdfs_log: "HDFS.log"
  templates: "HDFS.log_templates.csv"
  time_order: "Time_Order.csv"
